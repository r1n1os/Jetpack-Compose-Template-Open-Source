package com.r1n1os.jetpackcomposetemplateopensource.presentation.ui_models

/**
 * This is an example of a UI model for this template.
 * In your case if you prefer you can keep each UI model in a separate file(i.e In the same file as the UI/screen files).
 * */
data class ExampleUIModel(
    val name: String,
    val age: Int,
)
